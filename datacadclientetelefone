SELECT
    CLI.ID, CLI.FANTASIA, CLI.DATACAD AS DATA_CADASTRO, CLI.DDD, CLI.TELEFONE
FROM
    CADCLIENTE CLI
WHERE
    CLI.DATACAD IS NOT NULL
    AND CLI.DATACAD BETWEEN :DATA_INICIO AND :DATA_FIM
ORDER BY
    CLI.DATACAD